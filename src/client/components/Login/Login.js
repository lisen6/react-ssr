import React from 'react'

const Login = () => {
  return (
    <div>
      <div>LoginPage</div>
      <button onClick={() => console.log('login')}>login</button>
    </div>
  )
}

export default Login
